<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd">

	<!-- 实验12：引用外部属性文件 （Spring管理连接池）-->
	<!-- 数据库连接池作为单实例是最好的；一个项目就一个连接池，连接池里面管理很多连接，连接是直接从连接池里拿 -->
	<!-- 可以让Spring帮我们创建连接池对象（管理连接池） -->
	<!-- 加载外部配置文件  固定写法classpath: 表示引用类路径下的一个资源 -->
	<!-- 
		jdbc.username = root
		jdbc.password = 774891
		jdbc.url = jdbc:mysql://localhost:3306/stu_info?characterEncoding=utf-8
		jdbc.driver = com.mysql.jdbc.Driver
	 -->
	<context:property-placeholder location="classpath:dbconfig.properties"/>
	<bean id="comboPooledDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource">
		<!-- ${key}动态取出配置文件中某个key对应的值 
			 username是Spring中的一个关键字，不要用username
			 username是电脑的用户的名字  Administrator
		-->
		<property name="user" value="${jdbc.username}"></property>
		<property name="password" value="${jdbc.password}"></property>
		<property name="jdbcUrl" value="${jdbc.url}"></property>
		<property name="driverClass" value="${jdbc.driver}"></property>
	</bean>
	
	<bean class="com.wx.spring.bean.Car" id="car01">
		<!-- 会与后面的自动拼接成字符串 -->
		<property name="carName" value="${username}王小星"></property>
	</bean>
</beans>
